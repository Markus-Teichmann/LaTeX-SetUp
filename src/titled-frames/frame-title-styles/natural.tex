\renewcommand{\setFrameTitle}[1]{%
    \renewcommand{\getFrameTitle}{
        #1 \arabic{#1} \frameTitle %#1 ist der aus titled-framed-environment übergebene Name der Umgebung. z.B. Aufgabe
    }
}
