\renewcommand{\setFrameTitle}[1]{%
    \renewcommand{\getFrameTitle}{
        #1 \thesection{} \frameTitle %#1 ist der aus titled-framed-environment übergebene Name der Umgebung. z.B. Aufgabe
    }
}
